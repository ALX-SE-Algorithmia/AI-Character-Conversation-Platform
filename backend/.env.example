# Application
APP_NAME=AI Character Backend
APP_VERSION=0.1.0
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Providers and secrets (fill in as needed, do not commit real keys)
GROQ_API_KEY=
HF_API_TOKEN=
VECTOR_STORE_PATH=data/vectorstore
